# Turso (libSQL) database — used for the global leaderboard.
# Create with: turso db create collapsar && turso db tokens create collapsar
TURSO_DATABASE_URL=libsql://collapsar-yourname.turso.io
TURSO_AUTH_TOKEN=your-token-here
